using UnityEngine;

public class Test_A_code : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("Fatto Cacca Potente");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
